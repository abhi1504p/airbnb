import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors

  static const Color textcolor = Colors.white;

  // Dark Theme Colors

  static var errorColor=Color(0xFF0E0E0E);

  // Light Theme Colors
  static const lightPrimary = Color(0xFF4CAF50);         // Fresh green
  static const lightBackground = Color(0xFFF5F5F5);     // Very light gray
  static const lightSecondary = Color(0xFF03A9F4);      // Light blue accent

  // Dark Theme Colors
  static const darkPrimary = Color(0xFF121212);         // Almost black
  static const darkBackground = Color(0xFF1F1F1F);      // Dark gray
  static const darkSecondary = Color(0xFFBB86FC);       // Purple accent
}
